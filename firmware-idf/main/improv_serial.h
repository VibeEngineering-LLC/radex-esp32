#pragma once

/* #RADEX-186 шаг Б: Improv Serial — настройка Wi-Fi платы из браузера через
   USB-порт (improv-wifi.com/serial, Web Serial API). Первое подключение без
   captive-портала: страница открывает COM-порт, спрашивает состояние платы и
   передаёт SSID с паролем.

   Шаг А (флешер пишет сеть прямо в раздел NVS при прошивке) остаётся основным
   для чистой платы; Improv нужен там, где прошивка уже стоит, а сеть сменилась
   и в эфире её ещё нет — портал в такой ситуации требует пересесть на AP платы,
   а провод уже воткнут.

   Транспорт — UART0, тот же, куда идут логи: клиент ищет в потоке сигнатуру
   "IMPROV" и на строки лога не реагирует. */

void improv_serial_start(void);
