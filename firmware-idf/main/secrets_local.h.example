// Шаблон. Скопировать в secrets_local.h и вписать свою сеть.
// secrets_local.h в git НЕ попадает (.gitignore) — в нём пароль открытым текстом.
#pragma once
#define RADEX_GW_WIFI_SSID "your-ssid"
#define RADEX_GW_WIFI_PASS "your-password"
